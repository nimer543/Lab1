services:
  web:
    container_name: travel_planner_mvc
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./travels.db:/app/travels.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
